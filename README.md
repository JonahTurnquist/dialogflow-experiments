To run in dev mode, just run `npm run start-dev`. This will start the service on `localhost:3000`

To open a tunnel to connect DialogFlow to the locally running instance, use ngrok: `./ngrok http 3000 --region au`
